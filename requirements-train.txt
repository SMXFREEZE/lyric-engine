# Training stack (src/training/*) — SFT, genre LoRAs, RLHF/PPO.
# Install on top of requirements.txt:  pip install -r requirements-train.txt

trl>=0.9.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
datasets>=2.19.0
