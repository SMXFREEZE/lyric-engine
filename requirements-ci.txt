# Minimal deps for the pure-Python test suite (tests/ runs without torch —
# model-dependent paths are mocked or skipped).

pytest>=8.0.0
numpy>=1.26.0
pronouncing>=0.2.0
nltk>=3.8.1
textblob>=0.18.0
fastapi>=0.111.0
httpx>=0.27.0
