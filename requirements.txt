# Core — model inference, scoring, API serving, data annotation/collection.
# System dependency (Linux): espeak-ng for phonetic fallback experiments —
#   sudo apt-get install espeak-ng   (it is NOT a pip package)
# Optional stacks: requirements-audio.txt (song rendering),
#                  requirements-train.txt (fine-tuning / RLHF).

# ML
torch>=2.1.0
transformers>=4.43.0
peft>=0.11.0

# Phonetics / NLP scoring
pronouncing>=0.2.0
nltk>=3.8.1
textblob>=0.18.0
sentence-transformers>=3.0.0
scikit-learn>=1.4.0

# Data collection (Genius / charts)
lyricsgenius>=3.0.1
billboard.py>=2.5.0
requests>=2.31.0

# Serving
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Utils
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
python-dotenv>=1.0.0
rich>=13.7.0
typer>=0.12.0
